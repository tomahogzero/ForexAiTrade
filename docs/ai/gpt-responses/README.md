# GPT Responses

Use this folder to store important GPT review responses or external review notes.

Recommended naming:

- `YYYYMMDD-checkpoint-<letter>-response.md`
- `YYYYMMDD-pr-<number>-review-response.md`

When a response requires action, create or update a task under `docs/ai/tasks/`.
