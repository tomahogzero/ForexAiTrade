# Checkpoint FO Execution Notes

The runner validates frozen FJ, FK, FL, FM, and FN provenance, recomputes all FN metrics directly from FL canonical rows, applies the frozen Wilson method and decision rules, and writes deterministic machine-readable reports.

Two complete runs produced byte-identical JSON artifacts with mismatch count 0. No events or outcomes were regenerated or changed.