# Checkpoint DV Chronological Stability Map

Date: 2026-07-11

DV maps all 20 committed diagnostic windows without running MT5. The fixed classification yields 4 weak, 2 watch, and 14 normal windows.

The only consecutive weak pair is CY-W3 to DB-W1. DR-W1 and DI-W3 are isolated weak windows. The absolute historical stability gate therefore remains `FAIL`.

The latest 6 chronological windows contain no weak window; the latest 8 contain one isolated weak window and no consecutive pair. These are observations only because no trailing gate has been defined or approved.

Weak windows contain 16 gaps. Committed evidence attributes 6 to `PRICE_BETWEEN_EMAS` and 5 to `TREND_ALIGNMENT_CONFLICT`; DI-W3's 5 per-window gap reasons remain unresolved at summary level.

Rule-candidate and order-logic gates remain `FAIL`. PAF remains `NOT_READY_FOR_ORDER_LOGIC`.
